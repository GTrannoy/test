vcc		-- c_id_3 can take the values (gnd, vcc, sd0, sd1)
gnd		-- c_id_2 can take the values (gnd, vcc, sd0, sd1)
gnd		-- c_id_1 can take the values (gnd, vcc, sd0, sd1)
vcc		-- c_id_0 can take the values (gnd, vcc, sd0, sd1)
gnd		-- m_id_3 can take the values (gnd, vcc, sd0, sd1)
vcc		-- m_id_2 can take the values (gnd, vcc, sd0, sd1)
vcc		-- m_id_1 can take the values (gnd, vcc, sd0, sd1)
gnd		-- m_id_0 can take the values (gnd, vcc, sd0, sd1)
0		-- nostat: nanoFIP status enabled(0), nanoFIP status disabled(1)
010		-- produced variable length: 2 bytes(000), 8 bytes(001), 16 bytes(010), 32 bytes(011), 64 bytes(100), 124 bytes(101)
00		-- rate: 31.25 kbits(00), 1 Mbit(01), 2.5 Mbits(10)
1		-- mode (slone): memory mode(0), stand alone(1)
5A		-- station_adr (8-bit bus in hexadecimal format)
2000 ms	-- time for which the configuration above is valid
